# Python 3.9 compatible requirements for Ansible Molecule testing

# Ansible stack - version pinned for consistency with Python 3.9 compatibility
ansible==6.7.0
ansible-compat==3.0.2
ansible-core==2.13.13
ansible-lint==6.8.7
